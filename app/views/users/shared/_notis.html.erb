
      <!-- user list-->
      <% @notifications.each do |noti| %>
      <div class= "md2">
        <hr class = "original-h1"/>
          <div class="self-introduction"><h5>通知</h5>
            
            <hr class = "original-h1">
            <p>
              <%= noti.n_comment %>
              <%= simple_time(noti.created_at) %>>
              <p>
              <%= link_to @commenter, user_path(@commenter_id) %>
              さんが　<%= link_to @micropost.content, micropost_comments_path(@micropost.id) %> にコメントしてくれました。
              </p>
            </p>
        </div>
        </div>
      <% end %>
      
      
      <!-- paginate -->
      <div class="self-introduction"><p><%= will_paginate @notifications %></p></div>
      </div>
      
    </div>
  </div>